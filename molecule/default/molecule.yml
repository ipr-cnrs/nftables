---
dependency:
  name: galaxy
driver:
  name: docker
platforms:
  - name: instance
    image: centos/systemd:latest
    privileged: true
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup:ro
provisioner:
  name: ansible
verifier:
  name: ansible
